package moysklad

import (
	"net/url"
)

func GetEntity(entity string, filter url.Values) {
	/*base := API_BASE + entity

	url, err := url.Parse(base)
	if err != nil {log.Fatal(err)}

	query := url.Query()
	query.set*/

}
